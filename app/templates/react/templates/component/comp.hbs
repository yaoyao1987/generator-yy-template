import React, { FC, memo } from 'react';
import classNames from 'classnames';

/**
 * {{pascalCase name}}  {{CN}}
 * {{description}}
 */
export const {{pascalCase name}}: FC = memo((props) => {
  return <>{{pascalCase name}}</>;
});

export default {{pascalCase name}};